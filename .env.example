# Environment Variables for Server Health Check Application

# SECURITY: Generate a secure random key for production
# Generate one with: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-to-a-secure-random-key-in-production

# Flask environment (development or production)
FLASK_ENV=production

# Optional: Custom port (default: 5000)
# PORT=5000

# Optional: Max file upload size in bytes (default: 1MB)
# MAX_CONTENT_LENGTH=1048576

# Optional: Max servers per file (default: 100)
# MAX_SERVERS_PER_FILE=100
